export enum KEY_CODE {
  BACKSPACE = 8,
  TAB = 9,
  ENTER = 13,
}

export enum ERROR_CODE {
  NOT_FOUND = 404,
  FORBIDDEN = 403,
}

export enum COMMENT_TYPE {
  QUESTION = 1,
  COMMENT = 2,
  REPLY = 3,
}
